program circle;
var
	{Missing begin of compound statement}
	r, a, p, b : real; 
	i, j : integer := 0;	

	if j=0 then a := 1;
	r := 8;
	p := 0;
	a := 0;
	k := -1;
	if r > 5 then
	  a := (3.14) * r * r;
	if (a < 100) then
	    p := 2 * 3.14 * b; 
	{Display the results}
	writeln ( 'The output results are ' , r, p, b);
end.  
